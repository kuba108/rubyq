{
  "size": "<%= "#{params[:json][:size]}" %>",
  "align": "<%= "#{params[:json][:align]}" %>",
  "style": "<%= "#{params[:json][:style]}" %>",
  "label": "<%= "#{params[:json][:label]}" %>",
  "icon": "<%= "#{params[:json][:icon]}" %>"
}
