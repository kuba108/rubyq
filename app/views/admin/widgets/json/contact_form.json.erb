{
  "target_email":"<%= "#{params[:json][:target_email]}" %>",
  "label_name":"<%= "#{params[:json][:label_name]}" %>",
  "label_email":"<%= "#{params[:json][:label_email]}" %>",
  "label_text":"<%= "#{params[:json][:label_text]}" %>"
}
