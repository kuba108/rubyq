<%= form_for widget, url: admin_widget_path(id: widget.id), method: :put, remote: true do |f| %>
  <%= f.hidden_field :page_id, value: page.id %>
  <%= f.fields_for :json do |fj| %>
    <div class="widget-wysiwyg">
      <%= fj.hidden_field :html, value: raw(json['html']), id: 'wysiwyg-html' %>
      <trix-editor input="wysiwyg-html" class="wysiwyg-widget-editor"></trix-editor>
    </div>
  <% end %>
<% end %>